name: rule-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/rule-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    # Prevent a workflow_dispatch from a non-main branch from publishing to data.norge.no.
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/rule-type/rule-type-nb.html -a lang=nb vocabularies/rule-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/rule-type/rule-type-en.html -a lang=en vocabularies/rule-type/main-en.adoc
      host: https://data.norge.no
      ontology: rule-type
      ontology-type: vocabulary
      files: |
        vocabularies/rule-type/rule-type.ttl text/turtle en
        vocabularies/rule-type/rule-type-nb.html text/html nb
        vocabularies/rule-type/rule-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
