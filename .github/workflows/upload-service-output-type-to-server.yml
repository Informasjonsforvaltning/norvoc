name: service-output-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/service-output-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/service-output-type/service-output-type-nb.html -a lang=nb vocabularies/service-output-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/service-output-type/service-output-type-en.html -a lang=en vocabularies/service-output-type/main-en.adoc
      host: https://data.norge.no
      ontology: service-output-type
      ontology-type: vocabulary
      files: |
        vocabularies/service-output-type/service-output-type.ttl text/turtle en
        vocabularies/service-output-type/service-output-type-nb.html text/html nb
        vocabularies/service-output-type/service-output-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
