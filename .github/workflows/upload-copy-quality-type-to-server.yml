name: copy-quality adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/copy-quality-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    # Prevent a workflow_dispatch from a non-main branch from publishing to data.norge.no.
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/copy-quality-type/copy-quality-type-nb.html -a lang=nb vocabularies/copy-quality-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/copy-quality-type/copy-quality-type-en.html -a lang=en vocabularies/copy-quality-type/main-en.adoc
      host: https://data.norge.no
      ontology: copy-quality-type
      ontology-type: vocabulary
      files: |
        vocabularies/copy-quality-type/copy-quality-type.ttl text/turtle en
        vocabularies/copy-quality-type/copy-quality-type-nb.html text/html nb
        vocabularies/copy-quality-type/copy-quality-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
