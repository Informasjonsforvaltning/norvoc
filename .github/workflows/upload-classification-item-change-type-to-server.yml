name: classification-item-change-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/classification-item-change-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    # Prevent a workflow_dispatch from a non-main branch from publishing to data.norge.no.
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/classification-item-change-type/classification-item-change-type-nb.html -a lang=nb vocabularies/classification-item-change-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/classification-item-change-type/classification-item-change-type-en.html -a lang=en vocabularies/classification-item-change-type/main-en.adoc
      host: https://data.norge.no
      ontology: classification-item-change-type
      ontology-type: vocabulary
      files: |
        vocabularies/classification-item-change-type/classification-item-change-type.ttl text/turtle en
        vocabularies/classification-item-change-type/classification-item-change-type-nb.html text/html nb
        vocabularies/classification-item-change-type/classification-item-change-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
