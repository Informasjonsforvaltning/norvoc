name: criterion-requirement-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/criterion-requirement-type/**

jobs:
  adoc_build_and_upload_files:
    runs-on: ubuntu-latest
    name: asciidoctor build
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - name: Build html bokmål
        id: adocbuild-nb
        uses: Analog-inc/asciidoctor-action@master
        with:
          shellcommand: "asciidoctor -a data-uri -o vocabularies/criterion-requirement-type/criterion-requirement-type-nb.html -a lang=nb vocabularies/criterion-requirement-type/main-nb.adoc"
      - name: Build html english
        id: adocbuild-en
        uses: Analog-inc/asciidoctor-action@master
        with:
          shellcommand: "asciidoctor -a data-uri -o vocabularies/criterion-requirement-type/criterion-requirement-type-en.html -a lang=en vocabularies/criterion-requirement-type/main-en.adoc"

      - name: Upload files to static-rdf-server
        uses: Informasjonsforvaltning/upload-files-to-static-rdf-server-action@84beeeacd364f310ef6a253bc079354cdf62e5dd
        id: upload-files
        with:
          ontology-type: "vocabulary"
          ontology: "criterion-requirement-type"
          host: "https://data.norge.no"
          api-key: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
          files: |
            vocabularies/criterion-requirement-type/criterion-requirement-type.ttl text/turtle en
            vocabularies/criterion-requirement-type/criterion-requirement-type-nb.html text/html nb
            vocabularies/criterion-requirement-type/criterion-requirement-type-en.html text/html en

