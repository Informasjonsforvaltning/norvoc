name: criterion-requirement-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/criterion-requirement-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/criterion-requirement-type/criterion-requirement-type-nb.html -a lang=nb vocabularies/criterion-requirement-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/criterion-requirement-type/criterion-requirement-type-en.html -a lang=en vocabularies/criterion-requirement-type/main-en.adoc
      host: https://data.norge.no
      ontology: criterion-requirement-type
      ontology-type: vocabulary
      files: |
        vocabularies/criterion-requirement-type/criterion-requirement-type.ttl text/turtle en
        vocabularies/criterion-requirement-type/criterion-requirement-type-nb.html text/html nb
        vocabularies/criterion-requirement-type/criterion-requirement-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
