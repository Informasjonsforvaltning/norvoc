name: upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/public-service-type/**

jobs:
  adoc_build_and_upload_files:
    runs-on: ubuntu-latest
    name: asciidoctor build
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - name: Build html bokmål
        id: adocbuild-nb
        uses: Analog-inc/asciidoctor-action@master
        with:
          shellcommand: "asciidoctor -a data-uri -o vocabularies/public-service-type/public-service-type-nb.html -a lang=nb vocabularies/public-service-type/main-nb.adoc"
      - name: Build html english
        id: adocbuild-en
        uses: Analog-inc/asciidoctor-action@master
        with:
          shellcommand: "asciidoctor -a data-uri -o vocabularies/public-service-type/public-service-type-en.html -a lang=en vocabularies/public-service-type/main-en.adoc"

      - name: Upload files to static-rdf-server
        uses: Informasjonsforvaltning/upload-files-to-static-rdf-server-action@84beeeacd364f310ef6a253bc079354cdf62e5dd
        id: upload-files
        with:
          host: "https://data.norge.no"
          api-key: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
          ontology-type: "vocabulary"
          ontology: "public-service-type"
          files: |
            vocabularies/public-service-type/public-service-type.ttl text/turtle en
            vocabularies/public-service-type/public-service-type-nb.html text/html nb
            vocabularies/public-service-type/public-service-type-en.html text/html en
            
