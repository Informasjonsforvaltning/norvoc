name: provenance-activity-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/provenance-activity-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    # Prevent a workflow_dispatch from a non-main branch from publishing to data.norge.no.
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/provenance-activity-type/provenance-activity-type-nb.html -a lang=nb vocabularies/provenance-activity-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/provenance-activity-type/provenance-activity-type-en.html -a lang=en vocabularies/provenance-activity-type/main-en.adoc
      host: https://data.norge.no
      ontology: provenance-activity-type
      ontology-type: vocabulary
      files: |
        vocabularies/provenance-activity-type/provenance-activity-type.ttl text/turtle en
        vocabularies/provenance-activity-type/provenance-activity-type-nb.html text/html nb
        vocabularies/provenance-activity-type/provenance-activity-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
