name: legal-resource-type adocbuild and upload files to server

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - vocabularies/legal-resource-type/**
  workflow_dispatch:

jobs:
  adoc_build_and_upload_files:
    name: Upload vocabulary to static-rdf-server
    if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'
    uses: Informasjonsforvaltning/workflows/.github/workflows/specification-upload-files.yaml@main
    with:
      program_html: |
        asciidoctor -a data-uri -o vocabularies/legal-resource-type/legal-resource-type-nb.html -a lang=nb vocabularies/legal-resource-type/main-nb.adoc
        asciidoctor -a data-uri -o vocabularies/legal-resource-type/legal-resource-type-en.html -a lang=en vocabularies/legal-resource-type/main-en.adoc
      host: https://data.norge.no
      ontology: legal-resource-type
      ontology-type: vocabulary
      files: |
        vocabularies/legal-resource-type/legal-resource-type.ttl text/turtle en
        vocabularies/legal-resource-type/legal-resource-type-nb.html text/html nb
        vocabularies/legal-resource-type/legal-resource-type-en.html text/html en
    secrets:
      STATIC_RDF_SERVER_API_KEY: ${{ secrets.STATIC_RDF_SERVER_API_KEY }}
